spring:
  application:
    name: spring-app
  datasource:
    url: jdbc:postgresql://localhost:5432/time_tracking_systems
    username: ob11to
    password: ob11to
    driver-class-name: org.postgresql.Driver
  jpa:
    properties.hibernate:
      show_sql: true
      format_sql: true